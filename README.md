# ICMP Scanner

🔔 ICMP Scanner

## Usage

```shell
❯ ./icmp_scanner.py -h
usage: icmp_scanner [-h] -t TARGET [-a]

ICMP Scanner: Discover active hosts in a network

options:
  -h, --help            show this help message and exit
  -t TARGET, --t TARGET
                        Host or network address (CIDR) to scan. Ex: -t 192.168.1.1 |
                        --target 192.168.1.0/24
  -a, --all             Display all hosts, not only the active
```
